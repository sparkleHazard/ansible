---
network_interface: "eth0" # Adjust as necessary (e.g. eth0, wlan0)
static_ip: "192.168.1.8" # Desired static IP address
cidr: "24" # CIDR notation (24 means 255.255.255.0)
netmask: "24"
gateway: "192.168.1.1" # Gateway address
dns_primary: "192.168.1.1" # Primary DNS server
dns_secondary: "9.9.9.9" # Secondary DNS server
